"""Shared callback handlers."""

from aiogram import Router

router = Router()

# Additional callback handlers can be added here as the bot grows.
# For now, the main callbacks (ref_, rej_) are handled in admin.py.
